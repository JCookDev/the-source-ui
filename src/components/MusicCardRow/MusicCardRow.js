import React from 'react';

const MusicCardRow = ({ musicCard }) => {
    return(
        <tr>
            <td>
                {musicCard}
            </td>
        </tr>
    );
};

export default MusicCardRow;